# リポジトリガイドライン

## プロジェクト構成とモジュール配置

このリポジトリは現在、空のプロジェクト雛形です。本番コードは `src/`、
テストは `tests/`、静的ファイルやフィクスチャは `assets/` または
`tests/fixtures/` に配置してください。設定ファイルはリポジトリ直下に置きます。
プロジェクトの成長に合わせて、`src/auth/` のように機能単位でコードをまとめ、
各機能の公開エントリーポイントは小さく明確に保ってください。

## ビルド、テスト、開発コマンド

現時点ではビルドツールやパッケージマニフェストは登録されていません。導入時は、
標準コマンドを `README.md` に記載し、次のような予測可能なスクリプトを提供してください。

- `npm run dev` — ローカル開発を開始します。
- `npm test` — 自動テスト一式を実行します。
- `npm run lint` — フォーマットと静的解析を確認します。
- `npm run build` — 本番用ビルドを作成します。

生成物、依存関係ディレクトリ、認証情報、ローカル環境用ファイルはバージョン管理に追加しないでください。

## コーディングスタイルと命名規則

プロジェクトで採用したフォーマッターとリンターに従い、それらが管理するファイルを
手作業で整形しないでください。JSON、YAML、JavaScript、TypeScript は、言語標準で別の
慣習がない限り 2 スペースでインデントします。変数と関数には `camelCase`、コンポーネント、
クラス、型には `PascalCase`、`user-profile.ts` のようなファイル名には `kebab-case` を使います。

意味が明確な名前、小さく責務を絞ったモジュール、明示的なエラー処理を優先してください。
コメントはコードの繰り返しではなく、意図や自明でないトレードオフを説明するために使います。

## テスト方針

振る舞いを変更する際は必ずテストを追加してください。テストは `tests/` 配下、またはそれが
プロジェクトの慣例になった場合は対象モジュールの隣に置き、`user-profile.test.ts` のように命名します。
通常の動作、境界値、報告済みの不具合をカバーしてください。変更を提出する前に、テストとリンターを
すべて実行します。

## コミットとプルリクエストの方針

まだ Git 履歴がないため、コミット件名は `Add user profile validation` のように短い命令形で記述します。
コミットは目的を一つに絞ってください。プルリクエストには変更内容と理由、関連 Issue、実行した検証を
記載し、UI の変更にはスクリーンショットを添付します。設定、移行、セキュリティへの影響は明示してください。
